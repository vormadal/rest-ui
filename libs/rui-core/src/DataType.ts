export enum DataType {
  STRING = 'string',
  DATE = 'date',
  DATE_TIME = 'dateTime',
  TIME = 'time',
  NUMBER = 'number',
  BUTTON = 'button',
  BOOLEAN = 'boolean',
  LOOKUP = 'lookup',
  OBJECT = 'object',
  ARRAY = 'array'
}
